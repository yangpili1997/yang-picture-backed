server:
  port: 8123
  servlet:
    context-path: /api
spring:
  application:
    name: yang-picture-backend
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yang_picture
    username: root
    password: root
